const greet = (message, name) => {
    console.log(`${message}, ${name}!`)
  }
  
  greet('Hello', 'John');
  greet('Hola', 'Antonio');
  greet('Ciao', 'Luigi')


//   it uses the arrow function to make it more shorter 
//   and there is no posibility to make it more clean